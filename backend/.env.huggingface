# Hugging Face Space Environment Variables
# These should be set in the Space settings

# Database URL (optional - will use SQLite if not provided)
# DATABASE_URL=postgresql+asyncpg://user:password@host/db

# CORS Origins (will be updated after Vercel deployment)
CORS_ORIGINS=*
