# In a real-world application, you should generate a secure secret and store it in your environment variables.
# You can generate a new secret on the command line with:
# openssl rand -hex 32
SECRET_KEY=a_very_secret_key