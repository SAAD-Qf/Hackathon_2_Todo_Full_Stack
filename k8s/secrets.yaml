apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: todo-app
type: Opaque
stringData:
  # IMPORTANT: Replace with your actual Neon Postgres connection string
  # Format: postgresql+asyncpg://user:password@host/database
  DATABASE_URL: "postgresql+asyncpg://user:password@ep-example.us-east-2.aws.neon.tech/neondb"

  # For SQLite (development only)
  # DATABASE_URL: "sqlite+aiosqlite:///./todo.db"
